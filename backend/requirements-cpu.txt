# CPU-only AI Configuration - Complete Dependencies
# Tối ưu hóa cho môi trường production với CPU

# FastAPI Core
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
pydantic-settings==2.1.0
python-multipart==0.0.9

# Database
sqlalchemy==2.0.25
pymysql==1.1.0
cryptography==42.0.2
alembic==1.13.1

# Redis & Caching
redis==5.0.1
hiredis==2.3.2

# Authentication & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-dotenv==1.0.1

# Excel Processing
pandas==2.2.0
openpyxl==3.1.2
xlsxwriter==3.1.9

# Core ML dependencies (CPU optimized)
scikit-learn==1.4.0
numpy==1.26.4

# PyTorch CPU-only version (nhỏ hơn ~1GB so với GPU version)
torch==2.2.0+cpu
torchvision==0.17.0+cpu
torchaudio==2.2.0+cpu
-f https://download.pytorch.org/whl/torch_stable.html

# NLP libraries optimized for CPU
sentence-transformers==2.3.1
transformers>=4.32.0,<5.0.0
tokenizers>=0.13.0,<0.15.0

# Vietnamese NLP (lightweight)
underthesea==6.8.0

# Validation & Utils
email-validator==2.1.0
python-dateutil==2.8.2

# Logging & Monitoring
loguru==0.7.2
sentry-sdk[fastapi]==1.40.3

# Additional optimizations
# Note: This excludes heavy GPU dependencies and reduces Docker image size by ~1-2GB
# Background tasks (Celery) excluded for lightweight deployment
# Development tools excluded for production
